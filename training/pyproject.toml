[project]
name = "training"
version = "0.1.0"
description = "HUD RL training loop for the treefarm project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "hud-python>=0.6.6",
]
